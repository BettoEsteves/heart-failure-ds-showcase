terraform {
  required_version = ">= 1.6.0"
}

# Purpose: baseline Terraform entrypoint for infrastructure modules.
# Projects should add providers, backends, and modules based on environment policy.
